version: '3'
services:
  browserless:
    image: 'ghcr.io/browserless/chromium:v2.23.0'
    restart: unless-stopped
    environment:
      - TOKEN=***      - MAX_CONCURRENT=10
      - CONCURRENT_FREE=5
